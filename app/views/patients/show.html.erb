<%= @patient.name %><br><br>

Appointments:<br>
<% @patient.appointments.each do |a| %>
    <%= a.appointment_datetime.strftime("%B %e,  %Y at %H:%M") %> with <%= link_to a.doctor.name, doctor_path(a.doctor) %><br>
<% end %>